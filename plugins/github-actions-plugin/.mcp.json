{
  "mcpServers": {
    "github-actions": {
      "command": "npx",
      "args": ["-y", "github-actions-mcp"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}
