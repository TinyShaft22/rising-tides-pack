# Docker Plugin Connectors

## Required MCP
- **docker** — Container management, image operations, Docker Compose workflows

## Prerequisites
- Docker installed and running
- Python 3.12+ (for uvx)
- Docker socket accessible

## Environment Variables
- `DOCKER_HOST` (optional) — Remote Docker via SSH: `ssh://user@host`
